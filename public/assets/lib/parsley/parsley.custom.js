/**
 * Created by ASUS on 08.04.2014.
 */
/*window.ParsleyConfig = {
    validators: {
        csvdomains: {
            fn: function (value, requirement) {
                alert(value);
                return true;
                //return /^([a-z0-9]+([\-\.]{1}[a-z0-9]+)*\.[a-z]{2,6})(,([a-z0-9]+([\-\.]{1}[a-z0-9]+)*\.[a-z]{2,6}))*$/
                //    .test(value);
            },
            priority: 32
        }
    },
    i18n: {
        en: {
            csvdomains: 'Please, enter comma-separated domain names. Example: "gmail.com" or www.yahoo.com,google.com'
        }
    }
};*/
/*
window.ParsleyConfig = {
    validators: {
        multiple: {
            fn: function (value, requirement) {
                return 0 === value % requirement;
            },
            priority: 32
        }
    },
    i18n: {
        en: {
            multiple: 'This value should be a multiple of %s'
        },
        fr: {
            multiple: 'Cette valeur doit être un multiple de %s'
        }
    }
};*/
